q1)Build a Data Center Topology as shown below using Mininet CLI (DatacenterBasicTopo). In this topology, we have four brackets, each with four forces and one top-of-rack (ToR) switch. These ToR switches are connected to a central root switch.
